# С предыдущей командой, запретить все остальные
iptables -P INPUT DROP
iptables -P OUTPUT DROP

# Следующая команда удалит любой пакет, поступающий с IP-адреса: iptables -I INPUT -s {IP-HERE} -j DROP
# если ничего не указано, все будет запрещено, кроме того, что было разрешено
