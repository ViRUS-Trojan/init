nslookup slash16.org 8.8.8.8
# Запрашиваем внешний DNS-сервер по доменному имени.
# nslookup используется для запроса системы доменных имен (DNS) для получения доменного имени или сопоставления IP-адреса.